System.register(['angular2/testing', 'angular2/core'], function(exports_1, context_1) {
    "use strict";
    var __moduleName = context_1 && context_1.id;
    var testing_1, core_1;
    var db, MyService, MyMockService;
    return {
        setters:[
            function (testing_1_1) {
                testing_1 = testing_1_1;
            },
            function (core_1_1) {
                core_1 = core_1_1;
            }],
        execute: function() {
            class MyService {
            }
            class MyMockService {
            }
            // #docregion describeIt
            testing_1.describe('some component', () => {
                testing_1.it('does something', () => {
                    // This is a test.
                });
            });
            // #enddocregion
            // #docregion fdescribe
            testing_1.fdescribe('some component', () => {
                testing_1.it('has a test', () => {
                    // This test will run.
                });
            });
            testing_1.describe('another component', () => { testing_1.it('also has a test', () => { throw 'This test will not run.'; }); });
            // #enddocregion
            // #docregion xdescribe
            testing_1.xdescribe('some component', () => { testing_1.it('has a test', () => { throw 'This test will not run.'; }); });
            testing_1.describe('another component', () => {
                testing_1.it('also has a test', () => {
                    // This test will run.
                });
            });
            // #enddocregion
            // #docregion fit
            testing_1.describe('some component', () => {
                testing_1.fit('has a test', () => {
                    // This test will run.
                });
                testing_1.it('has another test', () => { throw 'This test will not run.'; });
            });
            // #enddocregion
            // #docregion xit
            testing_1.describe('some component', () => {
                testing_1.xit('has a test', () => { throw 'This test will not run.'; });
                testing_1.it('has another test', () => {
                    // This test will run.
                });
            });
            // #enddocregion
            // #docregion beforeEach
            testing_1.describe('some component', () => {
                testing_1.beforeEach(() => { db.connect(); });
                testing_1.it('uses the db', () => {
                    // Database is connected.
                });
            });
            // #enddocregion
            // #docregion beforeEachProviders
            testing_1.describe('some component', () => {
                testing_1.beforeEachProviders(() => [core_1.provide(MyService, { useClass: MyMockService })]);
                testing_1.it('uses MyService', testing_1.inject([MyService], (service) => {
                    // service is an instance of MyMockService.
                }));
            });
            // #enddocregion
            // #docregion afterEach
            testing_1.describe('some component', () => {
                testing_1.afterEach((done) => { db.reset().then((_) => done()); });
                testing_1.it('uses the db', () => {
                    // This test can leave the database in a dirty state.
                    // The afterEach will ensure it gets reset.
                });
            });
        }
    }
});
// #enddocregion
//# sourceMappingURL=testing.js.map